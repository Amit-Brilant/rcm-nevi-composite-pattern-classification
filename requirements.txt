# Pinned to the versions this code was written against (mid-2022).
# Several of these APIs have since changed or been removed; see NOTES.md.
# Every pin below was verified to resolve on PyPI.

torch==1.11.0
torchvision==0.12.0
torchmetrics==0.9.2
timm==0.6.5
kornia==0.6.5

albumentations==1.2.0
opencv-python==4.6.0.66
Pillow==9.1.1

numpy==1.22.4
pandas==1.4.2
scipy==1.8.1
scikit-learn==1.1.1

matplotlib==3.5.2
seaborn==0.11.2

tqdm==4.64.0
wandb==0.12.19

# Mosaic Annotation Viewer only. 4.60.x is the last generation usable
# without a licence key; 4.60.5.1 is the only 4.60 release still on PyPI.
PySimpleGUI==4.60.5.1
